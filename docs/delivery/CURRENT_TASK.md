# CURRENT TASK — 当前唯一工作包

任务编号：ACTIVE-AUTHORITY-R3
状态：唯一写入岗位已完成两阶段修改、构建与自测；三个既有只读岗位复测中，写入岗不得自行宣称最终交付
总控目标：在 COMBAT-AUTHORITY-R2 基线后，修复主动技能位移、事务、历史回溯与控制投射物的权威运行时缺口。

## 本轮唯一写入范围

- 真源：`prototype/play/game.js`、`game.css`、`delivery-v2.css`、`index.html`。
- QA：新增本工作包针对性验证器与 `prototype/play/evidence/active-authority-r3/`。
- 独立版：仅由 `prototype/play/build-delivery.mjs` 生成 `三命无常_V1完整交付.html`。
- 其他岗位只读；本岗位是上述文件族的唯一写入者。

## 附录 E 真值合同

1. H011：25m、3m宽冲锋；按路径首碰实体/墙停止，只命中首个目标，造成 `100+0.5ATK` 并眩晕0.8秒。
2. H013：40m可见目标；优先目标正后方1.5m，再按顺时针45度检查八候选；候选须通过玩家胶囊扫掠与落点合法性；无候选失败且不进CD；成功后武装下一次普攻暴击。
3. H014：15m胶囊扫掠闪现，在出发点留下1秒延迟诱饵，延迟后造成 `120+0.5ATK`。
4. H016：25m、3m宽穿透冲锋；路径目标各命中一次，造成 `100+0.5ATK` 并沿冲锋方向击退2m。
5. H021：1秒引导、15m半径，按 `THREAT_DESC / DISTANCE_MM_ASC / ENTITY_ID_ASC` 选最多8个合法目标；墙阻挡，首领/龙/核心Boss免疫；按角色朝向起始的8个独立2m环槽顺时针落位，槽占用时查找下一合法槽，否则使用最后合法扫掠点。
6. D13：50m两段选择，A/B不同；两个目标及两个目的地在提交瞬间全部合法才原子换位；任一失败、取消或超时均不进入CD。
7. D14：20Hz、6秒历史缓冲；选取“不晚于当前-5秒的最新合法存活样本”；生命结果为当前生命与历史生命（钳制到当前最大生命）的较高者；历史位置非法时保留当前位置，但不取消生命恢复；不回滚CD、金币、物品或状态。
8. H035：30m/s、60m、半径0.5m非追踪投射物；3D胶囊首碰，受静态实体、WIND_WALL、FROZEN_RIVER阻挡；命中定身2秒且仍允许普攻/主动。
9. D4：30m/s、45m非追踪投射物；首碰命中后变形2秒，禁普攻/主动、移速×1.3、保留移动类别并中断前摇/引导；既有己方实体照常存在。

## 已完成实现

- H011/H016 已接统一胶囊扫掠与路径实体命中：H011首实体/地形碰撞即停，只伤首个并晕0.8秒；H016路径目标各一次并沿冲刺方向击退2米。
- H013 已实现目标背后起始、顺时针45度八候选，候选需同时通过玩家胶囊扫掠与目的地合法性；全非法不进CD，成功武装且仅由下一次合法普攻消费必暴。
- H014 已解除召唤快捷分支短路，执行15米胶囊扫掠闪现，并在出发点生成1秒后真实结算伤害的诱饵实体。
- H021 已实现仅由实体作者字段 `threatScore/threat/damageThreat` 驱动的威胁降序、毫米距离升序、实体ID升序最多8目标选择；距离与直视不再污染第一排序键。落位使用从角色朝向开始的8个固定2米独立槽，非法槽向后轮询，否则取最后合法扫掠点。
- D13 已纳入玩家、普通/飞行/肥猪与可伤害召唤；双端目的地在动态墙、水域、地形与实体胶囊条件下预验证、提交后复验，失败回滚且不进CD。
- D14 已改为独立50ms定时器驱动的20Hz、6秒、121样本缓冲，不再依赖RAF帧率；选最新不晚于5秒前的合法存活样本，按 `max(当前HP, 历史HP)` 结算，历史点非法只放弃位置。
- D4/H035 已改为30m/s、0.5米半径非追踪弹体，复用统一连续首碰撞并受静态墙、风墙和冻河阻挡；同一碰撞时刻以墙优先、再按稳定实体ID决胜；D4变形保留移动×1.3并禁普攻/主动，H035仅禁移动。
- 第一阶段的6人同点独立命中、`actorIntent` 私密目标移除、D1 60m/s首碰、六控制闭环、110米镜头范围环及874×402可读性保持通过。
- 世界商人由无点击合同的 `button` 改为非交互展示实体，继续只通过近距交互键进入，消除语义与验证器误报。

## 验证与交接

1. 新增 `prototype/play/validate-active-authority-r3.mjs`，34/34。
2. R3 九个浏览器入口在真源和独立版均为9/9，判定依据为坐标、HP、控制、CD与落点实体数据；新增反例确认远处最高威胁P10优先、D14为20Hz/50ms独立调度、同距控制弹命中稳定ID P02；控制台错误/警告0。
3. 874×402逐个点击 P02–P07 均准确选中对应ID，P02/P07详情标题切换准确。
4. 浏览器岗复测发现“头像已切换但 `.selected` 慢一帧、怪物层可能抢点击”；已在目标提交内同步刷新玩家层，并将玩家层设为 `z-index:6` 高于PVE层4。源版与独立版在874×402无等待逐点点击 P02–P07 均即时得到 `selected=frame=点击ID`。
5. 两轮扩展回归均通过；最后一次独立版构建为2026-08-18 16:12:56，308项资源，离线验证通过。
6. 证据报告：`prototype/play/evidence/active-authority-r3/active-authority-r3-gates.md`。
7. 已通知 `三命无常｜视觉门禁验收岗`、`三命无常｜真源规则验收岗`、`三命无常｜真实浏览器QA岗` 对最新构建只读复测。

## 六道门禁

- Gate A 方向：写入岗完成；附录 E 工程键与审计表交叉核对，无方向冲突。
- Gate B 系统：写入岗自测通过；统一位移/碰撞、候选、严格威胁排序、原子事务、独立50ms采样和控制投射物稳定首碰已接线。
- Gate C 状态：写入岗自测通过；九项真实运行场景9/9，非反馈记录判定。
- Gate D 像素：写入岗完成100%/400%/800%检查，未见新增截边、黑线、光晕或接缝；待视觉岗独立裁决。
- Gate E 视口：写入岗完成1536×1024、1100×720、874×402；待视觉/浏览器岗独立裁决。
- Gate F 交付：真源与16:12:56独立版已同步、308项资源离线PASS、最新浏览器控制台错误/警告0、两轮扩展回归通过；待三个只读岗最终复测。

## 停止条件

- 三个只读岗位若报告阻断，继续由当前唯一写入者做最小修复、重建与复测。
- 连续两次修复未解决或连续三次工具失败则标记需要接管。
- 未经授权不扩展到本工作包之外的技能或文件族。
